#include "temp_eff_util/gateff.h"
#include "temp_eff_util/gatef2.h"

auto temp_eff( const int& matde, const int& sz2 ){
  if ( matde != 0 ){
    //gateff( );
    if (sz2 != 0 ){
     // gatef2( );
    }
  }

  return 1;


}

